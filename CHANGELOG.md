# Changelog

## v0.1.0 - unreleased

Read-only MCP observability server — exposes Laravel Swarm's `v0.19.0`
display-read seams as MCP **Resources** over the official `laravel/mcp` package,
so any MCP-compatible AI client can observe durable swarm runs. This first
release is deliberately read-only: operator control (pause/resume/cancel/signal)
is a later minor.

### Added

_To be filled in during release wrap-up._

### Changed

_To be filled in during release wrap-up._
